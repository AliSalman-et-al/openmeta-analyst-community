# Use rc_metastudio as the Python Package Name

The maintained Python application package should be named `rc_metastudio`. The shorter `rcms` form should be reserved for file extensions, environment variable prefixes, compact IDs, and CLI shorthand where appropriate.

This keeps Python imports explicit and aligned with Python package naming conventions while still allowing concise RC MetaStudio identifiers in places where brevity matters.

