# Delete Bundled Help and Help Access Together

The RC MetaStudio migration should delete the bundled legacy help directory and remove in-app Help access in the same phase. Removing only the UI access risks stale docs remaining packaged or discoverable; deleting only the docs risks broken Help actions.

New RC MetaStudio documentation should be created later as a separate documentation workstream.

