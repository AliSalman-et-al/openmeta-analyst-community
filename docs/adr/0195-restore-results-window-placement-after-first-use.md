# Restore Results Window Placement After First Use

Results Window will use Remembered Workspace Placement when valid for the current screen configuration and will default to maximized only when no remembered placement exists. This supersedes ADR 0188 only where that ADR requires every Results Window to open maximized; its authoritative fit-to-viewport routine, aspect-ratio preservation, reflow responsibility, and scale cap remain active.
