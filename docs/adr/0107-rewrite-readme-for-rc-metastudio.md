# Rewrite README for RC MetaStudio

`README.md` should be rewritten from scratch for RC MetaStudio rather than patched from the modernization-era OpenMeta[Analyst] Community README. It should introduce RC MetaStudio as the maintained product, link to `NOTICE.md` for provenance and affiliation disclaimers, document source setup through the package entry point, describe RCMetaR as the bundled private R package, use `.rcms` sample projects, and name verification lanes without `modern` or `legacy` framing.

Detailed modernization history belongs in ADRs and deeper `docs` pages, not in the README's primary narrative.

