# Rename Product-Era Python Classes

Python classes and symbols with product-era or unclear OpenMeta-derived names should be renamed during the mechanical layout migration. For example, `MetaForm` should become a current UI/domain name such as `MainWindow` if it represents the main desktop window.

Standard evidence-synthesis terms such as Meta-Regression should remain because they describe statistical concepts rather than product identity.

