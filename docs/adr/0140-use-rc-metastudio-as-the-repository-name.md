# Use rc-metastudio as the Repository Name

The maintained repository should ultimately be named `rc-metastudio`. Package metadata, README links, issue links, About/Legal links, and packaging metadata should target the final repository identity once the GitHub repository rename is complete.

The codebase should not depend on the checkout directory name, so the repository rename remains a metadata and hosting operation rather than an application behavior dependency.

