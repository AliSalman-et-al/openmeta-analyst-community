# Migrate Adaptive Layouts in Verified Archetype Slices

The rewrite will proceed through high-DPI and placement foundations, Workspace Windows, the Workflow Window and wizard pages, complex Transactional Dialogs, compact dialogs and Transient Windows, then final legacy deletion and application-wide audit. Legacy and replacement policies may coexist only for explicitly unmigrated forms, no new code may adopt legacy helpers, every slice must pass focused evidence before the next, and completion requires zero remaining geometry inference or counterproductive root-refit behavior.
