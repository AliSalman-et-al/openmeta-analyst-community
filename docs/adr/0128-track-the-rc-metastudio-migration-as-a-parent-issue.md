# Track the RC MetaStudio Migration as a Parent Issue

The RC MetaStudio migration should be tracked as one parent GitHub issue with linked phase issues for the implementation slices. The parent issue should link the migration plan and relevant ADRs; each phase issue should include concrete acceptance criteria, likely touched areas, and verification commands.

This keeps the broad migration visible without making any single issue or pull request too large to review.

