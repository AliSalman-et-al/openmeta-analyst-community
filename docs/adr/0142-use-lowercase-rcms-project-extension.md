# Use Lowercase rcms Project Extension

RC MetaStudio should use lowercase `.rcms` as the canonical project-file extension in docs, file dialogs, tests, packaging metadata, sample filenames, and generated files. The application may tolerate platform-provided uppercase variants where unavoidable, but maintained outputs and documentation should use `.rcms`.

This keeps the project-file identity consistent across platforms.

