# Do Not Ship Settings Migration

RC MetaStudio should not ship a migration path for old OpenMetaAnalyst settings, recent files, cache paths, or user configuration. The first RC MetaStudio release should create and use clean RC MetaStudio settings.

This keeps settings behavior aligned with the broader decision to remove reverse compatibility with OpenMeta[Analyst] product identity and `.oma` project files.

