# Hide Retained Future Feature Code Until Verified

RC MetaStudio may retain code for future feature areas such as network meta-analysis during the identity and layout migration, but unsupported workflows should remain hidden from user-facing UI and documentation until they have explicit verification and product documentation.

This prevents renamed-but-unverified code from becoming an accidental supported feature.

