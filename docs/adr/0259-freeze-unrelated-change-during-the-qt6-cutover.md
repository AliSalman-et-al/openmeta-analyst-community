# Freeze unrelated change during the Qt6 cutover

The repository enters an immediate feature freeze until the Native Qt6 Port release gates pass. Allowed changes are limited to the Qt6 migration, migration evidence and tests, first-release blockers, and statistically necessary corrections. Unrelated features, interface redesigns, dependency refreshes outside the locked port baseline, and broad refactors wait until after the first public Qt6 release.
