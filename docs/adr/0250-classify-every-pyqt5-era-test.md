# Classify every PyQt5-era test

Every PyQt5-era test will be inventoried and classified as ported to PyQt6, rewritten to preserve the same evidence at a stronger seam, or retired only when named stronger evidence makes it redundant. A Qt6 failure is not grounds for deletion, skipping, or permanent `xfail`; any intentional behavior change must pass the repository's existing documented compatibility-exception process.
