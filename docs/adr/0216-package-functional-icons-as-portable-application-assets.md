# Package Functional Icons as Portable Application Assets

Every Functional Icon will be stored as a self-contained application asset and bundled with RC MetaStudio rather than resolved from an operating-system icon font or remote icon set. ImageGen will create the specialized Statistical Concept Icon artwork, while deterministic Fluent-style geometry will keep familiar Action Icons optically consistent; both kinds must remain available in Windows, macOS, and Linux packages.
